@props(['id', 'name'])

<div>
    <h1>{{ $name }}</h1>
    
    <table>
        <thead>
            {{-- {{ $headers }} --}}
        </thead>

        <tbody>
            {{-- {{ $body }} --}}
        </tbody>
    </table>
</div>