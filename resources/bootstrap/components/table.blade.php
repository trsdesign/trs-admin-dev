@props(['id', 'name'])

<div>
    <h1>{{ $name }}</h1>
    
    <table class="table table-striped">
        <thead>
            {{-- {{ $headers }} --}}
        </thead>

        <tbody>
            {{-- {{ $body }} --}}
        </tbody>
    </table>
</div>